{
  "name": "@afauthhq/example-cli",
  "version": "0.0.2",
  "description": "Reference service-distributed CLI built on @afauthhq/agent — shared identity, self-provisioning signup, deferred claim",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "example-cli": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "test": "vitest run --passWithNoTests",
    "lint": "echo TODO: configure lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@afauthhq/agent": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
