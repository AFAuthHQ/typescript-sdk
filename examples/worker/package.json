{
  "name": "@afauthhq/example-worker",
  "version": "0.0.7",
  "description": "Reference Cloudflare Worker composing @afauthhq/server + @afauthhq/worker",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "test": "vitest run --passWithNoTests",
    "lint": "echo TODO: configure lint",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@afauthhq/core": "workspace:*",
    "@afauthhq/server": "workspace:*",
    "@afauthhq/worker": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240614.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.60.0"
  }
}
